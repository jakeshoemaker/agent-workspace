# Copy to .env (gitignored) and fill in. Never commit real values.
# AGENT_WORKSPACE_HOST=nuc
# TAILSCALE_HOSTNAME=nuc-agent
# Example only — real tokens belong in secrets/ or a password manager:
# OPENAI_API_KEY=
# GH_TOKEN=
