# local/ (machine-only)

Hostnames, LAN notes, personal Compose overrides, scratch. Not shared across machines.

Tracked: this README and `.gitkeep` only. Everything else should stay untracked or gitignored.
